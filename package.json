{
  "name": "claude-slack-bridge",
  "version": "0.1.0",
  "description": "Bridge Claude Code terminal sessions with Slack via MCP + tmux",
  "type": "module",
  "scripts": {
    "daemon": "node src/daemon.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@slack/bolt": "^4.3.0",
    "zod": "^3.24.0"
  }
}
